## Setup

1. Install dependencies: `npm install`

### iOS

2. Install CocoaPods dependencies:
`cd ios`
`pod install`
`cd ..`
3. Run the app: `npx react-native run-ios`

### Android

1. Make sure an Android emulator or device is running.
2. Run the app: `npx react-native run-android`
